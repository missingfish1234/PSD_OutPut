# PSD Export Handoff (2026-04-15)

## Current Build
- Plugin version: `1.1.64`
- Build: `v54`
- Stamp: `2026-04-14-03`
- Main file: `f:\pstesttool\Test-7ld4kd\index.js`

## Current User Issue (Not Solved Yet)
- Layer styles are still missing for some text layers, especially:
  - `images/TOTAL_WIN/CHT/UI_TotalWin_CHT_001.png` (`TOTAL WIN/CHT/總得分`)
- User also reported: numbers not converted to Smart Object may fail to export correctly.

## What The Latest Report Shows
- Export path is always fallback:
  - `strategy=duplicate-saveas-png`
  - `prepare=duplicate-isolate-bake-crop-saveAs`
- For failing text layer, report shows:
  - `kind=text`
  - `fillOpacity=0`
  - `smartObjectConverted=false`
  - `smartObjectError=""`
- This means text-layer smart-object conversion branch did **not** actually apply.

## Recent Code Changes Already In Place
- Quick export host failure cache (to avoid repeated slow failed attempts per layer):
  - `quickExportCapability` state and skip logic.
  - Location: `index.js` around `1574`.
- Recursive normal mode keeps folder hierarchy, but exports leaf layers (no group-composite export).
- Fallback preparation now supports:
  - smart object conversion attempt for text layers
  - fallback pixel materialization
  - detailed prepare info in debug
- `resolveDuplicatedLayer` now has compatibility checks + name/bounds fallback matching:
  - `isResolvedLayerCompatible`
  - `findBestLayerMatchByName`
  - `calculateLayerMatchScore`

## Critical Locations To Inspect Next
- Fallback entry:
  - `exportLayerViaDuplicateSaveAs` in `index.js` around `1717`
- Duplicate-layer resolution:
  - `resolveDuplicatedLayer` in `index.js` around `2729`
- Smart object conversion:
  - `maybeConvertLayerToSmartObjectForFallback` in `index.js` around `3105`
- Export debug text output:
  - `[export-debug]` line generation in `index.js` around `3528`
  - includes `so=` and `soErr=`

## Validation Files
- Latest metadata folder from user runs:
  - `d:\SLOT...\metadata` (user output folder)
- Check:
  - `export_report.txt`
  - `export_report.json`
  - `last_export_error.txt` (old quick-export failures are still useful context)

## Immediate Next Checks For New Window
1. Re-run export and inspect this line for `UI_TotalWin_CHT_001`:
   - `prepare=...`
   - `so=true/false`
   - `soErr=...`
2. If `so=false` again:
   - instrument `maybeConvertLayerToSmartObjectForFallback` to log why conversion gate is skipped.
   - verify `resolved.layer` is truly text layer after duplicate/resolve.
3. If `so=true` but style still missing:
   - inspect whether `isolateDocumentToLayerBranch` hides required sibling/style context before bake.

## Suggested Kickoff Prompt For New Window
Use skill `psd-export-uxp-handoff`, then continue fixing missing layer styles in `f:\pstesttool\Test-7ld4kd`.
Current build is `1.1.64 (v54)`.
Main failing case: layer `UI_TotalWin_CHT_001` (source path `TOTAL WIN/CHT/總得分`).
Latest report shows fallback path and `smartObjectConverted=false` despite text-layer conversion logic being added.
